const categoryRef = document.querySelectorAll('.item');

console.log(`В списке ${categoryRef.length} категории.`);

// const titleRef = document.querySelectorAll('h2');
// console.log(`В списке ${titleRef.length} категории.`);

// categoryRef.forEach(item => console.log(item));